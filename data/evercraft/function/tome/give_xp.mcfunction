# Tome of Experience — Give XP to player (macro)
# Called with storage evercraft:tome temp {stored_levels, stored_points}
$experience add @s $(stored_levels) levels
$experience add @s $(stored_points) points
