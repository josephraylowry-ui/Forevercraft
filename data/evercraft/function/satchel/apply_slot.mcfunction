# Satchel — Apply effects for a single stored artifact (macro)
# Args: artifact (artifact ID string like "glowstone_pendant")
# Dynamically calls the correct effect function
$function evercraft:satchel/effects/$(artifact)
