# Journey Pickaxe - Vein Destroy
# Destroys the ore at this position and drops loot with Fortune III

# Drop the ore loot using the pickaxe's fortune enchant
loot spawn ~ ~ ~ mine ~ ~ ~ mainhand

# Remove the block (no 'destroy' — prevents double drops)
setblock ~ ~ ~ air
