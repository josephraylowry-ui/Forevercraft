# Runeforge — Forge (LEGACY — replaced by state-based system)
# This function is no longer directly called.
# Forging now uses: start_forge.mcfunction (begin timer) + collect.mcfunction (apply rune)
# Kept as stub for safety — click_forge.mcfunction routes based on state.
